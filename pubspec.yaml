name: gad
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ^3.5.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  firebase_core: ^2.24.2
  cloud_firestore: ^4.14.0
  provider: ^6.0.0
  file_picker: ^5.2.6  # Para seleccionar archivos
  excel: ^2.0.6  # Para leer archivos Excel
  path_provider: ^2.0.14  # Para obtener rutas de almacenamiento

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
   - assets/images/